# Job type: gen_eval
# Generator evaluation with fixed solver (replay or regenerate mode).

APP_LABEL=gen-eval
PYTHON_MODULE=verl_inf_evolve.gen_eval.gen_eval
LOG_PREFIX=gen_eval

REQUIRED_DATA_PATHS=(
  ".cache/data/preprocessed/eval_documents.json"
  ".cache/data/preprocessed/curriculum_pool/supergpqa_science_800.json"
)

LAUNCH_MODE=hydra
HYDRA_CONFIG_KEY=gen_eval_experiment
JOB_NAME_PREFIX=ge

DEFAULT_GPU=8
DEFAULT_MEMORY=512
DEFAULT_CPU=32
DEFAULT_QUEUE=skypilot-jobs-8-gpu
