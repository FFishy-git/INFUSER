# Job type: sol_eval
# Solver benchmark evaluation (multi-checkpoint, multi-benchmark).

APP_LABEL=sol-eval
PYTHON_MODULE=verl_inf_evolve.sol_eval.sol_eval
LOG_PREFIX=sol_eval

REQUIRED_DATA_PATHS=(
  ".cache/data/preprocessed/benchmarks"
)

LAUNCH_MODE=hydra
HYDRA_CONFIG_KEY=sol_eval_experiment
JOB_NAME_PREFIX=se

DEFAULT_GPU=8
DEFAULT_MEMORY=512
DEFAULT_CPU=120
DEFAULT_QUEUE=skypilot-jobs-8-gpu
