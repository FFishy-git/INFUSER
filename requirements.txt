# Exported on 2026-06-07 from the pinned `verlai/verl` base image used by
# `launcher/opensource/Dockerfile.runtime`, after applying the runtime Python
# layer. This file is intended for the same base runtime family (or another
# environment that already provides the CUDA/PyTorch/vLLM/Ray stack).
#
# Notes:
# - Run from the repository root so the vendored `verl/` package is importable
#   via `PYTHONPATH`.
# - This file does not try to install the deep-learning base stack.
# - Optional code-benchmark dependencies still live in
#   `launcher/opensource/requirements-opencompass.txt`.

Pebble==5.1.0
aiohttp==3.13.5
appdirs==1.4.4
boto3==1.42.96
botocore==1.42.97
datasets==3.6.0
dill==0.3.8
fire==0.7.1
fsspec==2025.3.0
huggingface_hub==0.36.2
latex2sympy2_extended==1.11.0
math-verify==0.9.0
multipledispatch==1.0.0
multiprocess==0.70.16
openai==2.31.0
psutil==7.2.2
rich==14.3.3
s3transfer==0.16.1
stopit==1.1.2
tempdir==0.7.1
termcolor==3.3.0
tree-sitter-python==0.25.0
tree-sitter==0.25.2
wget==3.2
